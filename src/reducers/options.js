const initialState = {
    APIROOT: 'http://live.vanvikil.no/api/v1'
};

export default function options(state = initialState, action) {
    switch (action.type) {

    default:
        return state;
    }
}
